// give an example of for each

let arr = [1, 2, 3, 4, 5];
arr.forEach((num) => {
    console.log(num);
});

// 1

//make above example short
arr.forEach(num => console.log(num));

// The forEach() method executes a provided function once for each array element.
//difference between map and forEach is that map returns a new array, forEach does not return anything.